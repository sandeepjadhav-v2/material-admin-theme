# material-admin-theme
Angular Material Admin theme

## Prerequisites
To run the application on your local machine, make sure you have installed all of the following prerequisites on your development machine:
```bash
npm install -g @angular/cli
```
## Steps to run
1. git clone https://github.com/sandeepjadhav-v2/material-admin-theme.git
2. npm install
3. ng serve